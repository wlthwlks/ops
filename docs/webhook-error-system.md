# Webhook error system

Tables: `webhook_events`, `integration_errors` (migration `0004_forms_webhooks`).

OPS UI: `/ops/webhook-errors`

Stable codes in `src/lib/forms/errors.ts`.

Secrets redacted; passwords never stored.
